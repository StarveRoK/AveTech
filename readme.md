📞 Phone-Address Microservice

Микросервис для управления связками "телефон-адрес" с использованием FastAPI и Redis.

**Readme.md и tests - были созданы ИИ, и мной перепроверены**

## 🚀 Быстрый старт

### С использованием Docker Compose (рекомендуется)

```bash
# Клонируйте проект
git clone <repository-url>
cd phone-address-service

# Запустите сервисы
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

Сервис будет доступен по адресу: http://localhost:8000

### Без Docker

```bash
# 1. Установите Redis
# Linux (Ubuntu/Debian)
sudo apt-get update && sudo apt-get install redis-server

# macOS
brew install redis
brew services start redis

# Windows
# Скачайте Redis с https://github.com/microsoftarchive/redis/releases
# Или используйте WSL2

# 2. Установите Python зависимости
pip install -r requirements.txt

# 3. Запустите приложение
python main.py
```

## 📚 Документация API

После запуска сервиса документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### 1. Получить адрес по телефону
```http
GET /phones/{phone}
```

**Пример:**
```bash
curl "http://localhost:8000/phones/%2B79161234567"
```

**Ответы:**
- `200 OK` - телефон найден
```json
{
  "phone": "+79161234567",
  "address": "г. Москва, ул. Примерная, д. 1",
  "status": "success"
}
```
- `404 Not Found` - телефон не найден

#### 2. Создать новую запись
```http
POST /phones
```

**Пример:**
```bash
curl -X POST "http://localhost:8000/phones" \\
  -H "Content-Type: application/json" \\
  -d '{
    "phone": "+79161234567",
    "address": "г. Москва, ул. Примерная, д. 1"
  }'
```

**Ответы:**
- `201 Created` - запись создана
- `409 Conflict` - телефон уже существует

#### 3. Обновить адрес
```http
PUT /phones/{phone}
```

**Пример:**
```bash
curl -X PUT "http://localhost:8000/phones/%2B79161234567" \\
  -H "Content-Type: application/json" \\
  -d '{
    "address": "г. Санкт-Петербург, ул. Новая, д. 5"
  }'
```

**Ответы:**
- `200 OK` - адрес обновлен
- `404 Not Found` - телефон не найден

#### 4. Удалить запись
```http
DELETE /phones/{phone}
```

**Пример:**
```bash
curl -X DELETE "http://localhost:8000/phones/%2B79161234567"
```

**Ответы:**
- `204 No Content` - запись удалена
- `404 Not Found` - телефон не найден

### Дополнительные эндпоинты

#### Проверка здоровья
```bash
GET /health
```

#### Получение всех записей (админка)
```bash
GET /admin/records?limit=100
```

#### Статистика
```bash
GET /admin/stats
```

## 🐳 Docker команды

```bash
# Сборка и запуск
docker-compose up --build

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f api
docker-compose logs -f redis

# Пересборка
docker-compose build --no-cache

# Остановка с удалением volumes
docker-compose down -v

# Запуск в фоне
docker-compose up -d

# Проверка статуса
docker-compose ps
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Redis конфигурация
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Настройки приложения
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# TTL записей (в секундах)
RECORD_TTL=2592000  # 30 дней
```

### Параметры Redis

```python
# В коде можно настроить:
redis_manager = AsyncRedisManager(
    host='localhost',      # Хост Redis
    port=6379,            # Порт Redis
    db=0,                 # Номер базы данных
    max_connections=50,   # Максимальное количество соединений
    min_connections=2     # Минимальное количество соединений
)
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Установите зависимости для тестирования
pip install pytest pytest-asyncio

# Запуск всех тестов
pytest tests/ -v

# Запуск с покрытием
pytest tests/ --cov=main --cov-report=html

# Запуск конкретного теста
pytest tests/test_api.py::test_create_record_success -v
```

### Тестовые сценарии

```python
# Пример теста
def test_create_and_get_record():
    # Создание записи
    response = client.post("/phones", json={
        "phone": "+79161234567",
        "address": "Тестовый адрес"
    })
    assert response.status_code == 201
    
    # Получение записи
    response = client.get("/phones/%2B79161234567")
    assert response.status_code == 200
    assert response.json()["address"] == "Тестовый адрес"
```

## 📊 Мониторинг и логи

### Логирование

Логи доступны в контейнере по пути `/app/logs` или в stdout. Уровни логирования:
- `ERROR` - критические ошибки
- `WARNING` - предупреждения
- `INFO` - информация о работе
- `DEBUG` - отладочная информация

### Проверка здоровья

```bash
# Используйте встроенный скрипт
python health_check.py

# Или через curl
curl "http://localhost:8000/health"
```

## 🔒 Безопасность

### Валидация данных

1. **Телефоны**: нормализуются (удаляются все нецифровые символы кроме `+`)
2. **Адреса**: минимальная длина 5 символов
3. **TTL**: каждая запись имеет время жизни (30 дней по умолчанию)

### Обработка ошибок

- `400` - неверный запрос
- `404` - ресурс не найден
- `409` - конфликт данных
- `500` - внутренняя ошибка сервера
- `503` - сервис недоступен

## 🗄️ Архитектура

### Компоненты

```
phone-address-service/
├── main.py              # Основное FastAPI приложение
├── requirements.txt     # Зависимости Python
├── Dockerfile          # Конфигурация Docker
├── docker-compose.yml  # Docker Compose конфигурация
├── .env.example        # Пример переменных окружения
├── health_check.py     # Скрипт проверки здоровья
├── tests/              # Тесты
│   └── test_api.py
└── README.md          # Документация
```

### Поток данных

```
Клиент → FastAPI → AsyncRedisManager → Redis
        ↑          ↑
    Валидация   Кеширование
    (Pydantic)  (aioredis)
```

## 📈 Производительность

### Оптимизации

1. **Асинхронность**: весь код работает асинхронно
2. **Пул соединений**: Redis connection pool для эффективного использования ресурсов
3. **TTL**: автоматическая очистка старых записей
4. **Кеширование**: Redis как высокопроизводительное хранилище

### Бенчмарки

```bash
# Пример нагрузочного теста
docker run --rm --network host williamyeh/wrk \\
  -t12 -c400 -d30s http://localhost:8000/health
```

## 🚨 Устранение неисправностей

### Распространенные проблемы

1. **Redis недоступен**
   ```bash
   # Проверьте статус Redis
   docker-compose ps redis
   
   # Просмотрите логи
   docker-compose logs redis
   
   # Перезапустите Redis
   docker-compose restart redis
   ```

2. **Порт занят**
   ```bash
   # Найдите процесс использующий порт
   sudo lsof -i :8000
   sudo lsof -i :6379
   
   # Остановите процесс или измените порт в .env
   ```

3. **Ошибки подключения**
   ```bash
   # Проверьте переменные окружения
   docker-compose exec api env | grep REDIS
   
   # Проверьте сеть
   docker-compose exec api ping redis
   ```

### Логи

```bash
# Просмотр логов в реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs --tail=100 | grep ERROR

# Только API логи
docker-compose logs api --tail=50
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Добавьте тесты
4. Сделайте pull request

### Стандарты кода

```python
# Используйте type hints
def get_address(phone: str) -> Optional[str]:
    ...

# Документируйте функции
def create_record(data: PhoneAddressCreate) -> Dict[str, Any]:
    """
    Создает новую запись телефон-адрес.
    
    Args:
        data: Данные для создания записи
        
    Returns:
        Словарь с результатом операции
    """
```

## 📄 Лицензия

MIT License

## 📞 Поддержка

Для вопросов и поддержки:
- Создайте issue в репозитории
- Проверьте документацию по адресу http://localhost:8000/docs
- Используйте health check скрипт: `python health_check.py`

---

**🎯 Готово к production использованию!**
- [x] Docker контейнеризация
- [x] Полная документация API
- [x] Тесты
- [x] Мониторинг здоровья
- [x] Конфигурируемость
- [x] Логирование
- [x] Обработка ошибок

## 🎯 Пример использования

### 1. Создание записи через Python

```python
import aiohttp
import asyncio

async def create_phone_address():
    async with aiohttp.ClientSession() as session:
        data = {
            "phone": "+79161234567",
            "address": "г. Москва, ул. Примерная, д. 1"
        }
        
        async with session.post(
            "http://localhost:8000/phones",
            json=data
        ) as response:
            if response.status == 201:
                print("✅ Запись создана")
                return await response.json()
            else:
                print(f"❌ Ошибка: {response.status}")
                return None

# Запуск
asyncio.run(create_phone_address())
```

### 2. Bash скрипт для массового импорта

```bash
#!/bin/bash
# import_phones.sh

API_URL="http://localhost:8000/phones"

import_phone() {
    local phone="$1"
    local address="$2"
    
    curl -X POST "$API_URL" \\
        -H "Content-Type: application/json" \\
        -d "{\\"phone\\": \\"$phone\\", \\"address\\": \\"$address\\"}" \\
        --silent --show-error
    
    echo
}

# Импорт данных
import_phone "+79161234567" "Адрес 1"
import_phone "+79161234568" "Адрес 2"
import_phone "+79161234569" "Адрес 3"
```

Проект полностью готов к использованию! 🚀